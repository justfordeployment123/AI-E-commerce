# ─── API ──────────────────────────────────────────────────────────────────────
# URL of the backend API
NEXT_PUBLIC_API_URL=http://localhost:3002
# Production: NEXT_PUBLIC_API_URL=https://api.techstopuk.com

# ─── Object Storage ────────────────────────────────────────────────────────────
# Must match apps/api's GARAGE_PUBLIC_URL — used by proxy.ts (CSP img-src allowlist).
GARAGE_PUBLIC_URL=http://localhost:9000
# Production: GARAGE_PUBLIC_URL=https://storage.techstopuk.com

# ─── Scraper UI ───────────────────────────────────────────────────────────────
# Set to "true" to show competitor price scraping UI (buttons, pages, catalog price columns)
# Set to "false" (or omit) to hide all scraper-related UI
NEXT_PUBLIC_SCRAPER_ENABLED=true
# Production: NEXT_PUBLIC_SCRAPER_ENABLED=true
